diff --git a/node_modules/whatsapp-web.js/src/Client.js b/node_modules/whatsapp-web.js/src/Client.js
index 800109e..bbd1d17 100644
--- a/node_modules/whatsapp-web.js/src/Client.js
+++ b/node_modules/whatsapp-web.js/src/Client.js
@@ -490,18 +490,43 @@ class Client extends EventEmitter {
             referer: 'https://whatsapp.com/',
         });
 
-        await this.inject();
+        // PATCH micartaonline: WhatsApp Web navega a ?post_logout=1 en sesiones
+        // nuevas y recrea el contexto varias veces, destruyendo el inject inicial
+        // (la libreria solo re-inyectaba en framenavigated y sin tolerancia a
+        // fallos). Estrategia: inyectar con debounce tras la ULTIMA navegacion,
+        // con reintentos espaciados hasta que la pagina se estabilice.
+        let injectTimer = null;
+        const scheduleInject = () => {
+            clearTimeout(injectTimer);
+            injectTimer = setTimeout(async () => {
+                for (let i = 1; i <= 6; i++) {
+                    try {
+                        await this.inject();
+                        return;
+                    } catch (e) {
+                        console.error('[wwebjs inject intento ' + i + ']', (e.message || String(e)).split('\n')[0]);
+                        await new Promise((r) => setTimeout(r, 2500));
+                    }
+                }
+            }, 1500);
+        };
 
-        this.pupPage.on('framenavigated', async (frame) => {
-            if (frame.url().includes('post_logout=1') || this.lastLoggedOut) {
-                this.emit(Events.DISCONNECTED, 'LOGOUT');
-                await this.authStrategy.logout();
-                await this.authStrategy.beforeBrowserInitialized();
-                await this.authStrategy.afterBrowserInitialized();
+        this.pupPage.on('framenavigated', async () => {
+            // OJO: aqui la libreria original llamaba a authStrategy.logout() al
+            // ver ?post_logout=1, borrando el perfil del navegador MIENTRAS
+            // Chrome lo usa: corrompe su base local ("Ocurrio un error en la
+            // base de datos de tu navegador") y ademas emitia DISCONNECTED en
+            // sesiones nuevas (que aun no tienen nada que cerrar), matando el
+            // flujo de conexion. Solo tratamos como logout real el Cmd 'logout'
+            // de WhatsApp (lastLoggedOut).
+            if (this.lastLoggedOut) {
                 this.lastLoggedOut = false;
+                this.emit(Events.DISCONNECTED, 'LOGOUT');
             }
-            await this.inject();
+            scheduleInject();
         });
+
+        scheduleInject();
     }
 
     /**
@@ -1021,6 +1046,15 @@ class Client extends EventEmitter {
         );
 
         await this.pupPage.evaluate(() => {
+            // PATCH shiluv: ENGANCHAR LOS LISTENERS UNA SOLA VEZ POR CONTEXTO.
+            //
+            // Nada aqui quita los listeners anteriores, asi que cada llamada a
+            // attachEventListeners() anadia OTRO `Msg.on('add')` sobre los que ya
+            // habia. La marca vive en el contexto de la pagina: si navega, se
+            // pierden marca y listeners y la nueva inyeccion vuelve a enganchar.
+            if (window.__shiluvListenersAttached) return;
+            window.__shiluvListenersAttached = true;
+
             const { Msg, Chat } = window.require('WAWebCollections');
             const AppState = window.require('WAWebSocketModel').Socket;
 
diff --git a/node_modules/whatsapp-web.js/src/structures/Base.js b/node_modules/whatsapp-web.js/src/structures/Base.js
index ed1d83c..d3d0c46 100644
--- a/node_modules/whatsapp-web.js/src/structures/Base.js
+++ b/node_modules/whatsapp-web.js/src/structures/Base.js
@@ -19,6 +19,20 @@ class Base {
     _patch(data) {
         return data;
     }
+
+    /**
+     * WhatsApp Web renamed `_serialized` to `$1` on WID objects in July 2026.
+     * Keep the public whatsapp-web.js shape stable without mutating Store data.
+     *
+     * @param {object} id
+     * @returns {object}
+     */
+    static _normalizeId(id) {
+        if (id && id._serialized == null && id.$1 != null) {
+            return Object.assign({}, id, { _serialized: id.$1 });
+        }
+        return id;
+    }
 }
 
 module.exports = Base;
diff --git a/node_modules/whatsapp-web.js/src/structures/Chat.js b/node_modules/whatsapp-web.js/src/structures/Chat.js
index 40211bf..79624e3 100644
--- a/node_modules/whatsapp-web.js/src/structures/Chat.js
+++ b/node_modules/whatsapp-web.js/src/structures/Chat.js
@@ -19,7 +19,7 @@ class Chat extends Base {
          * ID that represents the chat
          * @type {object}
          */
-        this.id = data.id;
+        this.id = Base._normalizeId(data.id);
 
         /**
          * Title of the chat
diff --git a/node_modules/whatsapp-web.js/src/structures/Message.js b/node_modules/whatsapp-web.js/src/structures/Message.js
index dd977e8..db0179b 100644
--- a/node_modules/whatsapp-web.js/src/structures/Message.js
+++ b/node_modules/whatsapp-web.js/src/structures/Message.js
@@ -34,7 +34,7 @@ class Message extends Base {
          * ID that represents the message
          * @type {object}
          */
-        this.id = data.id;
+        this.id = Base._normalizeId(data.id);
 
         /**
          * ACK status for the message
@@ -74,7 +74,7 @@ class Message extends Base {
          */
         this.from =
             typeof data.from === 'object' && data.from !== null
-                ? data.from._serialized
+                ? (data.from._serialized || data.from.$1)
                 : data.from;
 
         /**
@@ -86,7 +86,7 @@ class Message extends Base {
          */
         this.to =
             typeof data.to === 'object' && data.to !== null
-                ? data.to._serialized
+                ? (data.to._serialized || data.to.$1)
                 : data.to;
 
         /**
@@ -95,7 +95,7 @@ class Message extends Base {
          */
         this.author =
             typeof data.author === 'object' && data.author !== null
-                ? data.author._serialized
+                ? (data.author._serialized || data.author.$1)
                 : data.author;
 
         /**
diff --git a/node_modules/whatsapp-web.js/src/util/Injected/Utils.js b/node_modules/whatsapp-web.js/src/util/Injected/Utils.js
index 540078a..28b2d36 100644
--- a/node_modules/whatsapp-web.js/src/util/Injected/Utils.js
+++ b/node_modules/whatsapp-web.js/src/util/Injected/Utils.js
@@ -713,18 +713,28 @@ exports.LoadUtils = () => {
             .require('WAWebPrepRawMedia')
             .prepRawMedia(opaqueData, mediaParams);
         const mediaData = await mediaPrep.waitForPrep();
-        const mediaObject = window
-            .require('WAWebMediaStorage')
-            .getOrCreateMediaObject(mediaData.filehash);
-        const mediaType = window.require('WAWebMmsMediaTypes').msgToMediaType({
-            type: mediaData.type,
-            isGif: mediaData.isGif,
-            isNewsletter: sendToChannel,
-        });
 
+        // WhatsApp Web convirtió `msgToMediaType` en un getter memoizado que
+        // desde septiembre de 2026 exige un mensaje con `id`. Aquí todavía no
+        // existe ningún mensaje: solo tenemos la media preparada, por lo que
+        // pasarlo por ese getter rompe TODOS los adjuntos de chats normales con
+        // "Data passed to getter must include an id property". Para esos chats
+        // se usa el tipo MMSv3 del objeto de media; `mediaData.type` contiene el
+        // texto crudo (por ejemplo, "video") y `castToV4` no acepta ese valor.
         if (!mediaData.filehash) {
             throw new Error('media-fault: sendToChat filehash undefined');
         }
+        const mediaObject = window
+            .require('WAWebMediaStorage')
+            .getOrCreateMediaObject(mediaData.filehash);
+        const MmsMediaTypes = window.require('WAWebMmsMediaTypes');
+        const mediaType = sendToChannel
+            ? MmsMediaTypes.msgToMediaType({
+                  type: mediaData.type,
+                  isGif: mediaData.isGif,
+                  isNewsletter: true,
+              })
+            : MmsMediaTypes.castToV4(mediaObject.type);
 
         if (
             (forceVoice && mediaData.type === 'ptt') ||
@@ -830,49 +840,63 @@ exports.LoadUtils = () => {
 
         if (typeof msg.id.remote === 'object') {
             msg.id = Object.assign({}, msg.id, {
-                remote: msg.id.remote._serialized,
+                remote: msg.id.remote._serialized || msg.id.remote.$1,
             });
         }
 
+        // WhatsApp Web renamed `_serialized` to `$1` for WIDs in July 2026.
+        if (msg.id && msg.id._serialized == null && msg.id.$1 != null) {
+            msg.id = Object.assign({}, msg.id, { _serialized: msg.id.$1 });
+        }
+
         delete msg.pendingAckUpdate;
 
         return msg;
     };
 
     window.WWebJS.getChat = async (chatId, { getAsModel = true } = {}) => {
-        const isChannel = /@\w*newsletter\b/.test(chatId);
-        const chatWid = window.require('WAWebWidFactory').createWid(chatId);
-        let chat;
+        try {
+            const isChannel = /@\w*newsletter\b/.test(chatId);
+            const chatWid = window.require('WAWebWidFactory').createWid(chatId);
+            let chat;
 
-        if (isChannel) {
-            try {
-                chat = window
-                    .require('WAWebCollections')
-                    .WAWebNewsletterCollection.get(chatId);
-                if (!chat) {
-                    await window
-                        .require('WAWebLoadNewsletterPreviewChatAction')
-                        .loadNewsletterPreviewChat(chatId);
-                    chat = await window
+            if (isChannel) {
+                try {
+                    chat = window
                         .require('WAWebCollections')
-                        .WAWebNewsletterCollection.find(chatWid);
+                        .WAWebNewsletterCollection.get(chatId);
+                    if (!chat) {
+                        await window
+                            .require('WAWebLoadNewsletterPreviewChatAction')
+                            .loadNewsletterPreviewChat(chatId);
+                        chat = await window
+                            .require('WAWebCollections')
+                            .WAWebNewsletterCollection.find(chatWid);
+                    }
+                } catch (ignoredError) {
+                    chat = null;
+                }
+            } else {
+                try {
+                    chat =
+                        window.require('WAWebCollections').Chat.get(chatWid) ||
+                        window.require('WAWebCollections').Chat.getModelsArray().find(c => (c.id._serialized || c.id.$1) === chatId) ||
+                        (
+                            await window
+                                .require('WAWebFindChatAction')
+                                .findOrCreateLatestChat(chatWid)
+                        )?.chat;
+                } catch (ignoredError) {
+                    chat = window.require('WAWebCollections').Chat.getModelsArray().find(c => (c.id._serialized || c.id.$1) === chatId) || null;
                 }
-            } catch (ignoredError) {
-                chat = null;
             }
-        } else {
-            chat =
-                window.require('WAWebCollections').Chat.get(chatWid) ||
-                (
-                    await window
-                        .require('WAWebFindChatAction')
-                        .findOrCreateLatestChat(chatWid)
-                )?.chat;
-        }
 
-        return getAsModel && chat
-            ? await window.WWebJS.getChatModel(chat, { isChannel: isChannel })
-            : chat;
+            return getAsModel && chat
+                ? await window.WWebJS.getChatModel(chat, { isChannel: isChannel })
+                : chat;
+        } catch (err) {
+            return null;
+        }
     };
 
     window.WWebJS.getChannelMetadata = async (inviteCode) => {
