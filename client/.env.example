# Copia este archivo como .env.production.local en /client
# Vercel también acepta estas variables en su panel de configuración (recomendado)

# URL del backend en Render (sin trailing slash, sin /api)
VITE_API_URL=https://tu-backend.onrender.com
